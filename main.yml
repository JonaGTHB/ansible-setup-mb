- hosts: localhost
  gather_facts: true
  roles:
   #- { role: ssh, tags: ["ssh"] }
   - { role: brew, tags: ["brew"] }
   - { role: nvim, tags: ["nvim"] }
   - { role: wm, tags: ["wm"] }
   - { role: terminal, tags: ["terminal"], gather_facts: true}
