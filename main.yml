- hosts: localhost
  gather_facts: true
  roles:
   - { role: nvim, tags: ["nvim"] }
   - { role: wm, tags: ["wm"] }
   - { role: ssh, tags: ["ssh"] }
   - { role: brew, tags: ["brew"] }
   - { role: terminal, tags: ["terminal"], gather_facts: true}
